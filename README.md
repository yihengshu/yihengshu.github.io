Please visit `https://yihengshu.github.io/`.

Run `python3 scripts/generate_content.py && python3 -m http.server 8000` to view locally at `http://localhost:8000/`.

`content.generated.md` is a generated file. It is built from `content.md`, with `{{LAST_UPDATED}}` replaced by the last git commit date of `content.md` before deployment.
